maptimeout 5
defc1 off

bind x remove
bind ^x remove
bind j focus down
bind k focus up
bind h focus left
bind l focus right
bind K kill remove
bind S eval split 'focus down' screen
bind | eval 'split -v' 'focus right' screen

startup_message off
ignorecase on
defscrollback 10000

layout autosave on
source .screen_layout
layout save default
